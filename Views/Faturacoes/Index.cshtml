@model IEnumerable<ProjetoPCRH.Models.Faturacao>

@{
    ViewData["Title"] = "Faturação";
}

<h1>Faturação</h1>

<p>
    <a asp-action="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataFatura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataFatura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contrato.ContratoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FaturaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.FaturaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.FaturaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
